# Windows送信側の遅延調査・改善引き継ぎ

2026-09-12。Windows側Codex向け。作業ブランチは **master**。

## 依頼

Macの受信スレッド起床待ちは改善したが、受信バッファ5msではまだ欠落がある。
「送信ワーカーが1つなので5ms以内に送り切れないのでは」という仮説を、
取得周期・キュー待ち・送信API内部・送信後の区間に分けて検証し、原因が確認できた箇所を改善してほしい。
ワーカー増加を最初から正解とせず、同条件で改善前後を実測すること。

## 現在の構成と観測

- WindowsはWi-Fiから有線LANへ切り替え済み。直近の送信元は192.168.11.29。
- Mac宛先は192.168.11.65:40100、受信インターフェースen8。
- MacはRME Fireface UCX II、48kHz、出力1/2、CoreAudio 128frames（約2.667ms）、ASRC有効、ゲイン−12dB。
- 比較時は受信バッファ5ms、映像同期の追加遅延0ms。IPや実行中設定は開始時に再確認する。
- 送信実装の既定は1ワーカー。現在実行中のWindowsプロセスの引数・GUI設定はWindows側で確認する。
- 約10ms分の480framesを取得し、128/128/128/96の4パケットへ分割して即時送信する。
  **128framesのパケットが約2.667msごとに生成される方式ではない。**
- 規則正しく10ms分がまとめて届けば、受信側の5msの余裕で再生できる可能性はある。
  10ms取得だから必ず10ms以上の追加受信バッファが必要、と断定しない。
  ただし取得・送信の遅延変動やCoreAudioへの受け渡し待ちが余裕を消費する。

Macの同一バイナリで各90秒×2回の比較：

| 指標 | 通常QoS | 改善後のリアルタイム受信 |
|---|---:|---:|
| カーネル到着→recvmsg完了 p99 | 1.927 / 2.714ms | 0.194 / 0.195ms |
| 同区間の最大 | 9.352 / 7.874ms | 0.274 / 0.368ms |
| 同区間1ms超のパケット数 | 885 / 1,288 | 0 / 0 |
| 欠落をゼロ補完した出力frames | 4,791 / 6,430 | 3,576 / 1,493 |
| underrunのあったcallback数 | 61 / 83 | 39 / 16 |

受信ログのヘッダー情報からWindowsの取得→送信API直前を再集計：

| リアルタイム受信試行 | 受信packet数 | p99 | 最大 | 1ms超 |
|---|---:|---:|---:|---:|
| 1 | 35,924 | 0.1545ms | 0.3778ms | 0 |
| 2 | 35,920 | 0.1577ms | 0.4828ms | 0 |

この約7.2万packetでは送信ワーカーの処理能力不足を示す兆候は薄い。
ただし、**届いたpacketだけの統計**であり、期限切れ・プール枯渇・送信失敗のpacketは含まれない。
またヘッダーのsend時刻はAPI呼び出し直前なので、API内部での停止やNICへの実送出待ちは含まれない。

RT試行1には、到着間隔が送信間隔より14.130ms拡大した例がある。
そのpacketの送信間隔は10.351ms、Mac受信待ちは0.084ms。
この例では送信API呼び出し以降→Macカーネル到着の区間が主な調査対象。
これだけでWindows・NIC・LAN・Macカーネルのどれが原因かは確定できない。

Macのキュー公開→CoreAudio取り出しもp99約2.65msあり、受信RT化だけでは消えない。
欠落frames/underrunsは再生側の不足を表し、UDPのネットワーク損失数ではない。

## 優先して調べる項目

### 1. 取得イベントとPCM取得の間隔

- 実際のbackend、対象PID、デバイス、サンプルレート、MMCSS登録結果を記録。
- `wake`、`capture`の時刻から、イベント間隔、非空イベント間隔、取得間隔、1回に排出したpacket数・framesを集計。
- 10ms周期が15〜20ms以上に伸びるとき、音声が複数ブロックまとめて取得されるか、欠落するか確認。
- GetBuffer所要時間、イベント起床→最初のGetBuffer、drain処理時間を分離。
- discontinuity、timestamp error、有効なdevice positionの飛び、session更新を記録。
- 無音・音源停止と処理停止を区別する。process loopbackの無効なdevice positionを欠落の根拠にしない。

### 2. キュー待ち、起床、送信API内部

既存の`apps/sender-windows/src/udp.rs`にある集計をまず利用する：

- `acquire_to_send_us`：取得→送信API直前。
- `publish_to_send_us`：キュー公開→送信API直前。
- `send_call_us`：送信APIの経過時間。nonblockingでもスケジューリングによる停止はあり得る。
- `deadline_drops`、`would_block_drops`、`send_errors`、プール枯渇、テレメトリ破棄。
- MMCSS登録エラー、ワーカーごとの送信数・CPU使用時間。

現行の5ms期限は「取得後5msを超えていればAPIを呼ばず破棄する」設定。
**API呼び出し完了やMac到着までを5ms以内に保証するものではない。**
旧通常優先度の試験ではAPI経過時間最大18.946msと期限切れ175件が記録されている。
MMCSS後・現在の有線LAN条件で再測定し、古い試験を現在の原因と決めつけない。

集計だけで原因を結び付けられない場合は、固定容量のメタデータ記録を追加する。
wireの`session_id`＋`sequence`＋`first_sample`をキーに、取得・公開・ワーカー起床・send開始/終了・結果・破棄理由を記録。
ホットパスでファイルI/Oや可変長割当を行わず、別スレッドで書き出す。ログ欠落も計数する。

### 3. 送信API以降とMac到着の対応付け

- WindowsログとMacの`--events`ログを同時取得し、wire session/sequenceで対応させる。
- sendが長い場合は、その前後のCPUスケジューリング、DPC/ISR、NICドライバー処理をETW/WPR等で絞って調査。
- sendが短いのに到着が遅れる場合は、可能ならWindows側のパケット観測とMacカーネル受信時刻を照合。
  パケットキャプチャ時刻も物理回線への送出時刻とは限らないので、観測地点を明記する。
- NICの省電力・割り込み集約・ドライバー等は、原因を示す計測が得られてから一項目ずつ比較する。
- Macの`arrival_excess_ms`は隣接到着packetの「受信間隔−send試行間隔」。
  順序逆転や欠落を考慮し、これを絶対片道遅延と呼ばない。
- Windows QPCとMac時計を直接引き算しない。Windows内の区間はQPC同士、Mac内はMac時計同士で計算。
  QPCのプロトコル値は100ns単位で、raw tickではない。

## 比較の進め方と判断

1. まず現在の1ワーカーで連続音声を使い、各90秒以上を複数回記録する。
   backend、音源、NIC、期限、packetサイズ、Mac側設定を固定する。
2. p50/p99/p99.9/最大、1ms・5ms超の件数、破棄・失敗・順序逆転・欠落、CPUを報告。
   全期間と起動直後を除いた区間を分け、都合の悪い最大値を除外しない。
3. キュー待ち・ワーカー停止が原因と確認できたら、起床方式やMMCSSの適用範囲を評価。
   必要なら1→2→2→1ワーカーの順で比較する。
4. 取得が詰まる場合はキャプチャ側の処理・スケジューリング・実際に利用可能な取得周期を調査。
   既存の低遅延backend試験で失敗した方式は、その記録を読んでから再評価する。
5. 原因に対応した変更後、Windowsでbuild/testを行い、同じLAN条件で再測定する。
   短い比較で改善しても、最後に10分以上の連続試験で再確認する。

過去のlocalhost試験では1ワーカーの取得→send p99は0.1776ms、最大0.2898ms、順序逆転0。
2ワーカーは順序逆転1,856件、3ワーカーは3,028件で、増やせば安定する結果ではなかった。
今回のLAN条件での再評価は可能だが、平均値だけの改善で採用しない。

意図的な送信待ちや大きなリングバッファを追加して低遅延目標を隠さない。
期限を伸ばす場合は診断用の比較条件として明記し、古い音声がMacで遅着になる影響も確認する。
取得周期を短くする案も、通知間隔だけでなく実際のPCM取得量と取得遅延を測る。

## 返してほしい成果物

- 確認したWindows実行設定、実行コマンド、コミット、計測時間。
- 各区間の分布・最大値・破棄理由別件数と、悪化したpacket前後の時系列例。
- 1ワーカー不足説について「支持／支持しない／未確定」と根拠。
- 改善したコード、Windowsでの検証結果、同条件での改善前後比較。
- Macと同時収録できなかった項目は未検証と明記し、必要なMac側操作を引き継ぐ。
- PCMは保存せずメタデータを利用。ローカルの巨大ログはGitへ追加せず、再現手順と集約結果を残す。

## 関連ファイル

- [Windows送信実装・過去の比較](docs/udp-sender.md)
- [Mac受信起床待ちの改善と測定](docs/receiver-wakeup.md)
- [Mac比較の集約JSON](docs/results/2026-09-12-receiver-wakeup.json)
- [テレメトリの定義](docs/telemetry.md)
- [プロトコル](docs/protocol.md)
- [Windows送信ワーカー](apps/sender-windows/src/udp.rs)
- [Windowsキャプチャ](apps/sender-windows/src/capture.rs)
- [Windows GUI](apps/sender-gui/Program.cs)
- [Mac計測スクリプト](scripts/benchmark-receiver-wakeup.py)

Macの生ログはMacローカル`runs/wakeup/ab/`にあり、Gitには含まれない。
GUIは通常ログでは1秒ごとの集計、CLIの`--events`でpacket単位の情報を出す。
同一ポートでGUIとCLI受信器を同時起動しないこと。
